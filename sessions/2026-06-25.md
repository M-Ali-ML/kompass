# Session Log: 2026-06-25

## 🎯 Current Objectives
- [x] Implement Phase 5 — Scenario Comparison & Stress Scoring: enrich the `Scenario` schema, add a `generate_scenarios` agent tool, and render side-by-side comparison cards in the frontend (inspired by a user-provided prototype).
- [x] Bring the post-build docs in `docs/` current (they were stuck at Phase 2).

## 🚀 Today's Progress
- **Phase 5 Backend — Schema**:
  - Enriched [scenario.py](file:///Users/aly/repos/kompass/backend/app/domain/scenario.py): added `CostBreakdown` (transportation / accommodation / grand_total) and `StressFactors` (layover_count, overnight_travel, tight_connection, total_travel_hours); added `comparison_label`, `start_date`/`end_date`, and `highlights`; replaced the flat `total_cost` with `cost_breakdown`.
  - Exported `CostBreakdown` / `StressFactors` from [domain/__init__.py](file:///Users/aly/repos/kompass/backend/app/domain/__init__.py).
- **Phase 5 Backend — Agent**:
  - Added the `generate_scenarios(destination, scenarios, estimated)` tool in [agent.py](file:///Users/aly/repos/kompass/backend/app/agent/agent.py). Architecture: the **agent builds** the 2-3 `Scenario` objects; the tool stays thin/deterministic — it recomputes each `grand_total` from its parts, threads the traveler's currency, flags `estimated`, and returns the render payload (`model_dump(mode="json")`).
  - Updated [system_prompt.md](file:///Users/aly/repos/kompass/backend/app/agent/prompts/system_prompt.md) with a "Scenario Comparison" directive and revised output-format guidance (compare → call `generate_scenarios`, single plan → return `Scenario`).
- **Phase 5 Backend — Tests**:
  - Updated the structured-output test in [test_agent.py](file:///Users/aly/repos/kompass/backend/tests/test_agent.py) for the new schema and added `test_generate_scenarios_normalizes_totals_and_payload` (verifies grand-total normalization + payload shape + ISO date serialization). Full suite: **25 passed**.
- **Phase 5 Frontend — Generative UI**:
  - Built [scenario-comparison-card.js](file:///Users/aly/repos/kompass/frontend/app/components/tool-cards/scenario-comparison-card.js): responsive side-by-side cards (row on desktop, stacked on mobile) with comparison label + date window, hero grand total, Transport/Stay breakdown, a 5-pip stress gauge (calm-green → busy-rose) with label (Relaxed → Intense), highlight chips, a transport-leg summary line, and a "View details" action. **Best value** (cheapest) and **Lowest stress** badges are derived client-side; shimmer skeleton while streaming.
  - Registered the `generate_scenarios` renderer in [use-trip-tools.js](file:///Users/aly/repos/kompass/frontend/app/hooks/use-trip-tools.js). `next build` compiles clean.
- **Docs**:
  - Brought [architecture.md](file:///Users/aly/repos/kompass/docs/architecture.md), [product.md](file:///Users/aly/repos/kompass/docs/product.md), [user-stories.md](file:///Users/aly/repos/kompass/docs/user-stories.md), and [design.md](file:///Users/aly/repos/kompass/docs/design.md) current through Phases 3–5 (persistence, flights MCP + grounded web search, scenario comparison). Fixed the stale "HSL color variables" wording in design.md (the variables are hex).
  - Updated [implementation_plan.md](file:///Users/aly/repos/kompass/artifacts/implementation_plan.md) to mark 5.1–5.3 done and note 5.4's remaining live check.

## ⚠️ Gotchas & Troubleshooting
- **Frontend was modularized mid-session**: `page.js` had been refactored into `app/hooks/` + `app/components/tool-cards/` (each card exports a zod `*Parameters` schema + a `*Card` component, registered in `use-trip-tools.js`). First draft of the comparison component was placed at the old `components/` path; moved it into `components/tool-cards/scenario-comparison-card.js` to match the new pattern.
- **Backend tests need `PYTHONPATH`**: run with `PYTHONPATH=. uv run pytest` from `backend/` (carried over from prior sessions; `python` alone is not on PATH — use `uv`).
- **Comparison card width**: the shared `ToolCard` shell is capped at `max-w-md`, too narrow for 3 cards; the comparison card uses its own wider container instead.

## 🧠 Preferences & Architectural Decisions
- **Agent-builds-scenarios (not tool-builds)**: per user choice, scenario itineraries are authored by the LLM (richer/creative) and the `generate_scenarios` tool only normalizes/presents. Keeps the tool deterministic and testable.
- **Stress gauge color is semantic, not brand**: gauge runs green→amber→rose by stress level (clearer for a stress metric) rather than the prototype's pink/purple. Flagged to the user as easily revertible.
- **Derived badges client-side**: "Best value" (min grand_total) and "Lowest stress" (min stress_score) are computed in the frontend, not stored on the model.
- **Design/Stitch**: produced a Stitch prompt for the comparison layout grounded in `globals.css` + `docs/design.md`; user shares both together (system doc + component prompt).

## 🔌 Flight data: removed `fli` + synthetic (reliability fix)
- **Why**: user noticed "approx" prices were synthetic. `fli` scrapes the *unofficial* Google Flights endpoint (no official API exists) and was blocked here → synthetic fallback presented fake-real numbers.
- **Research (2026)**: Amadeus Self-Service is being **decommissioned 17 Jul 2026** (avoid). Chose **SerpApi Google Flights** — structured JSON with price insights + cheapest-date grid, free 250/mo then $25/mo, legal ToS, ~15-min integration. Duffel = booking-oriented (overkill); Kiwi Tequila = prod needs approval. Keep `search_web` (Gemini grounding) as the always-on fallback.
- **Done now (interim, "strip synthetic")**: gutted [flights_server.py](file:///Users/aly/repos/kompass/backend/app/mcp_servers/flights_server.py) — both tools return `available: false` with no data; removed `_synthetic_*` + `_live_*` (fli) code; dropped `flights` from `pyproject.toml` and re-locked. Updated [system_prompt.md](file:///Users/aly/repos/kompass/backend/app/agent/prompts/system_prompt.md) (structured tools OFFLINE → use `search_web` for all prices) and [test_flights.py](file:///Users/aly/repos/kompass/backend/tests/test_flights.py) (dropped synthetic/live/retry tests; added unavailable + "estimators are gone" tests). **23 passed.**
- **Done (SerpApi wired)**: rewrote [flights_server.py](file:///Users/aly/repos/kompass/backend/app/mcp_servers/flights_server.py) on **SerpApi Google Flights** (httpx). `search_flights` = one-way, price-sorted, maps stops/`outbound_times`/currency/adults; `find_cheapest_dates` = bounded date sampling (`FLIGHTS_DATE_SAMPLES`, default 6; SerpApi has no future date-grid), one call per candidate date, keeps cheapest fare. Key `SERPAPI_API_KEY` added to [config.py](file:///Users/aly/repos/kompass/backend/app/config.py) + `.env`/`.env.example` and **exported to `os.environ`** so the MCP subprocess inherits it. Added `httpx` dep. Degrade contract: no key / error / no results → `available:false` → agent uses `search_web`. Tests: mocked SerpApi parsing + no-key degrade; **25 passed**.
- **⚠️ Action for user**: paste the real SerpApi key into `backend/.env` (`SERPAPI_API_KEY=`, currently a placeholder) and restart the backend. Live end-to-end not yet run (sandbox can't reach serpapi.com + key was placeholder).
- **Cost note**: `find_cheapest_dates` makes up to `FLIGHTS_DATE_SAMPLES` (default **4**) SerpApi calls per invocation; the agent typically runs it once per compared month, so a 2-month comparison ≈ 8 calls. Free tier is 250/mo. Lower `FLIGHTS_DATE_SAMPLES` further to save quota.
- **Verified live**: SerpApi returning real BER→NAP fares (e.g. €147–€254) end-to-end; scenarios rendered with `estimated=False` (no `approx` badge).

## 🧭 "View details" / full itinerary — DONE
- Built a centered **popup modal** ([scenario-comparison-card.js](file:///Users/aly/repos/kompass/frontend/app/components/tool-cards/scenario-comparison-card.js), `ScenarioDetailModal`) opened from "View details". Portals to `document.body` (escapes any ancestor transforms), Esc + scrim-click + X close, body-scroll lock.
- Sections (guided by the user's Stitch prototypes, desktop-only, **no loading state**): header (label + comparison_label pill + dates/nights), total price + stress gauge, highlight chips, cost breakdown (Transport/Stay proportional bars), stress factors (layovers/overnight/tight-conn/travel-time tiles), **travel timeline** (legs with times/carrier/duration + layover pills), **accommodation** cards (name/location/nights/cost), **itinerary highlights** accordion (day-by-day, first open), reasoning summary. `next build` green.
- User explicitly rejected the side-panel layout → centered popup instead.
- ⚠️ "Save this trip" button currently just closes the modal (visual placeholder; no save-scenario backend yet). → **superseded below.**

## 💾 "Save this trip" — real persistence (dedicated table)
- User chose a **dedicated, queryable `saved_scenarios` table** (multiple saves per conversation). Full hexagonal slice added:
  - Model [saved_scenario.py](file:///Users/aly/repos/kompass/backend/app/domain/models/saved_scenario.py) — stores full `Scenario` JSON + denormalized columns (`destination`, `grand_total`, `stress_score`, `start/end_date`, `currency`) and optional `trip_id` (FK `ON DELETE SET NULL`, nullable; saves outlive deleted conversations). Registered in `models/__init__.py`.
  - Port [saved_scenario_repository.py](file:///Users/aly/repos/kompass/backend/app/ports/saved_scenario_repository.py) + adapter [sqlite_saved_scenario_repository.py](file:///Users/aly/repos/kompass/backend/app/adapters/sqlite_saved_scenario_repository.py) (save / list[/by trip] / get / delete).
  - Routes: `POST /api/saved-scenarios`, `GET /api/saved-scenarios[?trip_id=]`, `GET|DELETE /api/saved-scenarios/{id}`.
  - Frontend: `saveScenario/listSavedScenarios/deleteSavedScenario` in [trips-api.js](file:///Users/aly/repos/kompass/frontend/app/lib/trips-api.js); modal "Save this trip" now POSTs (links to the active `thread_id` via `useAgent().agent.threadId`) with idle→saving→saved/error states (green ✓ on success).
  - Tests: saved-scenario lifecycle + denormalization + trip filter + minimal-defaults in [test_persistence.py](file:///Users/aly/repos/kompass/backend/tests/test_persistence.py). **27 backend tests pass**, `next build` green.
- **Saved browser UI — DONE**: sidebar now has a **Trips | Saved** tab toggle ([trip-sidebar.js](file:///Users/aly/repos/kompass/frontend/app/components/trip-sidebar.js)). New [saved-list.js](file:///Users/aly/repos/kompass/frontend/app/components/saved-list.js) lists saved scenarios (destination, comparison label + dates, price, stress dot), click → opens the **same detail popup** (now `export`ed from scenario-comparison-card.js) in `savedId` mode where the footer becomes **"Remove from saved"** (deletes + refreshes); trash icon for quick delete. `next build` green, lint clean.

## 📅 Comprehensive day-by-day itinerary
- Enriched `DaySummary` ([itinerary.py](file:///Users/aly/repos/kompass/backend/app/domain/itinerary.py)): added `title` + a `schedule` of time-blocked `PlanItem`s (`period`, `activity`, `details?`, `location?`), replacing the flat `activities: list[str]`. Exported `PlanItem` from `app.domain`.
- System prompt now instructs the agent to fill each trip day with a title, one-line description, and 3-5 concrete schedule items (morning→evening) with real places/tips, tailored to vibe/pace/seasonality.
- Modal `DayRow` renders title in the header and an expandable per-day timeline (period · activity · location · details). Updated test fixture to new schema. Backend 25 passed; `next build` green.
- Note: `pre-docs/product.md` had a "don't over-spoonfeed itineraries" scope note — overridden per explicit user request for richer days.

## 📋 Next Steps
- [ ] Live-verify SerpApi once the real `SERPAPI_API_KEY` is in `.env`: "cheapest BER→ATH in September" and a specific-date search should return real fares (no `approx` badge).
- [ ] Design (Stitch) → implement "View details" → expandable full itinerary (legs w/ times+carriers, accommodations, day-by-day).
- [ ] Live end-to-end verification of 5.4: "Compare a trip to Santorini in August vs September" → 2-3 cards, correct badges/currency.
- [ ] Begin Phase 6 — Accommodations MCP server (research API, build server, port+adapter, integrate accommodation costs into scenario grand totals).
